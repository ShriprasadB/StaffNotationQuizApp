# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Xcode user-specific
xcuserdata/
*.xcuserdatad/

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (if added later)
Pods/

# Carthage (if added later)
Carthage/Build/

# Fastlane (if added later)
fastlane/report.xml
fastlane/screenshots/

# Firebase service-account key — NEVER commit this
serviceAccount*.json
*-firebase-adminsdk-*.json
backend/serviceAccount.json

# GoogleService-Info.plist (safe to commit, but ignore by default)
# GoogleService-Info.plist
